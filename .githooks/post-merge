#!/bin/sh
# Keep vendored submodules (Dependency, Element, Documentation/Rust)
# tracking their remote branch tip after every pull/merge, so nobody has to
# remember to run `git submodule update --remote --recursive --merge` by hand.
git submodule update --init --remote --recursive --merge
